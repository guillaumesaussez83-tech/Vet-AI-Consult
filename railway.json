{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile && npm run build && npm run build:server"
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "startCommand": "node artifacts/api-server/dist/index.js"
  }
}
