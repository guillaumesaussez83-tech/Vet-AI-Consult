{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter ./artifacts/vetcare run build && pnpm run build:server"
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "startCommand": "node artifacts/api-server/dist/index.js"
  }
}
