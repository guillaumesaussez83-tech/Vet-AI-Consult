{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter ./artifacts/vetcare run build && pnpm --filter ./artifacts/api-server run build"
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "startCommand": "node artifacts/api-server/dist/index.js"
  }
}
